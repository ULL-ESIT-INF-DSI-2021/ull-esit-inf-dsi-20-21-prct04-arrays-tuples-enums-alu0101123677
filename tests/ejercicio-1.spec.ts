import 'mocha';
import {expect} from 'chai';
import {decodeResistor} from '../src/ejercicio-1';

describe('add function tests', () => {
  it('add(1, 8) returns value 9', () => {
    expect(decodeResistor('hola')).to.be.equal(2)
  });
});